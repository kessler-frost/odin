__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.worktrees/
.DS_Store
.odin/
.odin-v4d-test/
.odin-w21-test/
.odin-w24-test/
.odin-w27-test/
# W2.6 mesh integration tests: a throwaway store root under the repo tree,
# because Colima only shares $HOME into its VM and a backing's nebula sidecar
# bind-mounts its cert/config from the store (a /private/var tmp_path mount
# comes up empty inside the container).
.odin-mesh-it/
node_modules/
bun.lock
.vite/
.playwright-cli/
.playwright/
*.png
!assets/*.png
.odin-dev.log
.odin.pid
infra/
ui/infra/
ui/.odin-dev.log

# Local planning/design notes, not part of the repo
docs/plans/

# Claude Code machine-local settings (keep CLAUDE.md tracked)
.claude/settings.local.json

# Built UI bundled into the wheel at release time (not committed)
src/odin/_ui/
.superpowers/
