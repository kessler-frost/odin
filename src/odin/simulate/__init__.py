"""Simulate (S2): `tofu apply`/`destroy` through odin's gateway as a server
capability. See `odin.simulate.workspace` (TF workspace materialization) and
`odin.simulate.runner` (the tofu subprocess runner)."""
from __future__ import annotations
